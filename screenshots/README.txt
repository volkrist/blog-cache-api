Нужны 3 файла (перетащи сюда из папки с сохранёнными скринами):

  swagger.png       — Swagger UI (localhost:8000/docs)
  create_post.png   — ответ 201 на POST /posts
  tests.png         — вывод pytest (2 passed)

После добавления файлов выполни в корне проекта:
  git add screenshots
  git commit -m "fix: replace screenshots"
  git push
