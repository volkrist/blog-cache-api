Ожидаемые файлы для раздела Screenshots в README:

  swagger.png       — Swagger UI (localhost:8000/docs)
  create_post.png   — ответ 201 на POST /posts
  tests.png         — вывод pytest (2 passed)
  update_post.png   — (опционально) PUT /posts/{id}
